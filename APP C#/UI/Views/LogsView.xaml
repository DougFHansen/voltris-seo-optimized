<UserControl x:Class="VoltrisOptimizer.UI.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sipviews="clr-namespace:VoltrisOptimizer.Core.SystemIntelligenceProfiler.UI.Views"
             Background="{DynamicResource DarkBrush}"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
    
    <Grid>
        <!-- Background decorativo -->
        <Grid IsHitTestVisible="False" ClipToBounds="True">
            <Ellipse Width="350" Height="350" 
                     Fill="{DynamicResource SecondaryBlurBrush}"
                     HorizontalAlignment="Right" VerticalAlignment="Top"
                     Margin="0,-80,-120,0">
                <Ellipse.Effect>
                    <BlurEffect Radius="100"/>
                </Ellipse.Effect>
            </Ellipse>
            <Ellipse Width="300" Height="300" 
                     Fill="{DynamicResource PrimaryBlurBrush}"
                     HorizontalAlignment="Left" VerticalAlignment="Bottom"
                     Margin="-100,0,0,-100">
                <Ellipse.Effect>
                    <BlurEffect Radius="80"/>
                </Ellipse.Effect>
            </Ellipse>
        </Grid>
        
        <Grid Margin="20,16,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" MinHeight="300"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header Centralizado -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16" HorizontalAlignment="Center" Orientation="Horizontal">
                <StackPanel.RenderTransform>
                    <TranslateTransform/>
                </StackPanel.RenderTransform>
                <StackPanel.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-20" To="0" Duration="0:0:0.5">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </StackPanel.Triggers>
                
                <!-- Ícone moderno com gradiente -->
                <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0" VerticalAlignment="Center">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#14B8A6" Offset="0"/>
                            <GradientStop Color="#10B981" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Color="#14B8A6" Opacity="0.4"/>
                    </Border.Effect>
                    <Path Data="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20M13,13V18H10V13H13M14,13H17V18H14V13M7,13H10V18H7V13Z" 
                          Fill="White"
                          Width="18" Height="18"
                          Stretch="Uniform"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                </Border>
                
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Logs"
                              FontFamily="Segoe UI Variable Display, Segoe UI"
                              FontSize="24"
                              FontWeight="Bold">
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FF4B6B" Offset="0"/>
                                <GradientStop Color="#8B31FF" Offset="0.5"/>
                                <GradientStop Color="#31A8FF" Offset="1"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                    </TextBlock>
                    <TextBlock Text="Visualize os registros de atividades do sistema"
                              FontSize="11"
                              Foreground="{DynamicResource TextSecondaryBrush}"
                              Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Estatísticas de Logs -->
            <Border Grid.Row="1"
                    Background="{DynamicResource DarkPanelBrush}"
                    BorderBrush="{DynamicResource DarkBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="16,12"
                    Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Info Count -->
                    <Border Grid.Column="0" Margin="0,0,6,0" Padding="12,8" CornerRadius="8">
                        <Border.Background>
                            <SolidColorBrush Color="{DynamicResource PrimaryColor}" Opacity="0.1"/>
                        </Border.Background>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="InfoCountText" Text="0" FontSize="20" FontWeight="Bold"
                                      Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="INFORMAÇÃO" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" 
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Success Count -->
                    <Border Grid.Column="1" Margin="3,0" Padding="12,8" CornerRadius="8">
                        <Border.Background>
                            <SolidColorBrush Color="{DynamicResource SuccessColor}" Opacity="0.1"/>
                        </Border.Background>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="SuccessCountText" Text="0" FontSize="20" FontWeight="Bold"
                                      Foreground="{DynamicResource SuccessBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="SUCESSO" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" 
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Warning Count -->
                    <Border Grid.Column="2" Margin="3,0" Padding="12,8" CornerRadius="8">
                        <Border.Background>
                            <SolidColorBrush Color="{DynamicResource WarningColor}" Opacity="0.1"/>
                        </Border.Background>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="WarningCountText" Text="0" FontSize="20" FontWeight="Bold"
                                      Foreground="{DynamicResource WarningBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="AVISO" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" 
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Error Count -->
                    <Border Grid.Column="3" Margin="6,0,0,0" Padding="12,8" CornerRadius="8">
                        <Border.Background>
                            <SolidColorBrush Color="{DynamicResource ErrorColor}" Opacity="0.1"/>
                        </Border.Background>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="ErrorCountText" Text="0" FontSize="20" FontWeight="Bold"
                                      Foreground="{DynamicResource ErrorBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="ERRO" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" 
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            
            <!-- Filtros e Busca -->
            <Border Grid.Row="2"
                    Background="{DynamicResource DarkPanelBrush}"
                    BorderBrush="{DynamicResource DarkBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="16,12"
                    Margin="0,0,0,12">
                <Border.RenderTransform>
                    <TranslateTransform/>
                </Border.RenderTransform>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard BeginTime="0:0:0.1">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.5">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="200"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox x:Name="SearchTextBox"
                            Grid.Column="0"
                            Style="{StaticResource ModernTextBoxStyle}"
                            MinHeight="38"
                            Margin="0,0,16,0"
                            TextChanged="SearchTextBox_TextChanged"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox x:Name="FilterInfoCheckBox"
                                 Content="INFO"
                                 IsChecked="True"
                                 Foreground="{DynamicResource PrimaryBrush}"
                                 FontSize="11"
                                 FontWeight="SemiBold"
                                 Margin="0,0,12,0"
                                 Checked="FilterCheckBox_Changed"
                                 Unchecked="FilterCheckBox_Changed"
                                 Style="{StaticResource ModernCheckBoxStyle}"/>
                        
                        <CheckBox x:Name="FilterSuccessCheckBox"
                                 Content="SUCESSO"
                                 IsChecked="True"
                                 Foreground="{DynamicResource SuccessBrush}"
                                 FontSize="11"
                                 FontWeight="SemiBold"
                                 Margin="0,0,12,0"
                                 Checked="FilterCheckBox_Changed"
                                 Unchecked="FilterCheckBox_Changed"
                                 Style="{StaticResource ModernCheckBoxStyle}"/>
                        
                        <CheckBox x:Name="FilterWarningCheckBox"
                                 Content="AVISO"
                                 IsChecked="True"
                                 Foreground="{DynamicResource WarningBrush}"
                                 FontSize="11"
                                 FontWeight="SemiBold"
                                 Margin="0,0,12,0"
                                 Checked="FilterCheckBox_Changed"
                                 Unchecked="FilterCheckBox_Changed"
                                 Style="{StaticResource ModernCheckBoxStyle}"/>
                        
                        <CheckBox x:Name="FilterErrorCheckBox"
                                 Content="ERRO"
                                 IsChecked="True"
                                 Foreground="{DynamicResource ErrorBrush}"
                                 FontSize="11"
                                 FontWeight="SemiBold"
                                 Checked="FilterCheckBox_Changed"
                                 Unchecked="FilterCheckBox_Changed"
                                 Style="{StaticResource ModernCheckBoxStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Área de Logs - CORRIGIDO: Altura responsiva e sem scroll horizontal -->
            <Border Grid.Row="3"
                    Background="{DynamicResource DarkPanelBrush}"
                    BorderBrush="{DynamicResource DarkBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="0"
                    Margin="0,0,0,12">
                <Border.RenderTransform>
                    <TranslateTransform/>
                </Border.RenderTransform>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard BeginTime="0:0:0.2">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.5">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                
                <ListBox x:Name="LogsListBox"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="12"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        VirtualizingStackPanel.IsVirtualizing="True"
                        VirtualizingStackPanel.VirtualizationMode="Recycling"
                        ScrollViewer.CanContentScroll="True"
                        FontFamily="Cascadia Code, Consolas, monospace"
                        FontSize="11"
                        SelectionMode="Extended">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding FullText}" 
                                     Foreground="{Binding Color}"
                                     TextWrapping="Wrap"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Padding="4,2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource DarkPanelAltBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource DarkPanelAltBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
            
            <!-- Botões -->
            <StackPanel Grid.Row="4"
                       Orientation="Horizontal"
                       HorizontalAlignment="Right">
                <StackPanel.RenderTransform>
                    <TranslateTransform/>
                </StackPanel.RenderTransform>
                <StackPanel.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard BeginTime="0:0:0.3">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.5">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </StackPanel.Triggers>
                
                <Button x:Name="RefreshButton"
                       Content="🔄 Atualizar"
                       Style="{StaticResource NeonSecondaryButtonStyle}"
                       MinHeight="40"
                       Padding="18,0"
                       Click="RefreshButton_Click"
                       Margin="0,0,10,0"/>
                
                <Button x:Name="ClearLogsButton"
                       Content="🗑️ Limpar Logs"
                       Style="{StaticResource NeonSecondaryButtonStyle}"
                       MinHeight="40"
                       Padding="18,0"
                       Click="ClearLogsButton_Click"
                       Margin="0,0,10,0"/>
                
                <Button x:Name="ExportLogsButton"
                       Content="📥 Exportar Logs"
                       Style="{StaticResource NeonButtonStyle}"
                       MinHeight="40"
                       Padding="18,0"
                       Click="ExportLogsButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
