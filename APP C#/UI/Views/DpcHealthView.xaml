<UserControl x:Class="VoltrisOptimizer.UI.Views.DpcHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource DarkBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="DPC Health" FontSize="18" FontWeight="Bold"/>
            <Button Content="Support Pack" Margin="12,0,0,0" Click="OnSupportPack"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Grid.Column="0">
                <ItemsControl x:Name="Chart"/>
            </Border>
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock x:Name="StatsText"/>
                <TextBlock Text="Recommendation" FontWeight="Bold" Margin="0,8,0,4"/>
                <TextBlock x:Name="RecText"/>
            </StackPanel>
        </Grid>
        <ListView Grid.Row="2" x:Name="SpikesList" Height="160"/>
    </Grid>
</UserControl>
