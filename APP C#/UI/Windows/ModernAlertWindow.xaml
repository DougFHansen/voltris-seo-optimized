<Window x:Class="VoltrisOptimizer.UI.Windows.ModernAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aviso" Height="Auto" Width="520" MinHeight="280" MaxHeight="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" SizeToContent="Height"
        Topmost="True" ShowInTaskbar="False"
        RenderOptions.BitmapScalingMode="HighQuality"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
    
    <Border CornerRadius="20" 
            Margin="0"
            ClipToBounds="True"
            Background="#0A0A0F">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" 
                            ShadowDepth="0" 
                            Opacity="0.7" 
                            Color="Black"/>
        </Border.Effect>
        <Border.BorderBrush>
            <SolidColorBrush Color="#1E1E2E"/>
        </Border.BorderBrush>
        <Border.BorderThickness>
            <Thickness>1</Thickness>
        </Border.BorderThickness>
        
        <Grid>
            <!-- Borda sutil com gradiente -->
            <Border CornerRadius="20" 
                   BorderThickness="1"
                   IsHitTestVisible="False">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#25FFFFFF" Offset="0"/>
                        <GradientStop Color="#10FFFFFF" Offset="0.5"/>
                        <GradientStop Color="#05FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
            </Border>
            
            <Grid Margin="32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Ícone de Sucesso Moderno com cores Voltris -->
                <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <!-- Círculo de fundo com gradiente -->
                    <Ellipse Width="80" Height="80" Opacity="0.15">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#FF4B6B" Offset="0"/>
                                <GradientStop Color="#8B31FF" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    
                    <!-- Círculo principal com gradiente Voltris -->
                    <Ellipse Width="64" Height="64">
                        <Ellipse.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF4B6B" Offset="0"/>
                                <GradientStop Color="#8B31FF" Offset="0.5"/>
                                <GradientStop Color="#31A8FF" Offset="1"/>
                            </LinearGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    
                    <!-- Ícone de Check (✓) CENTRALIZADO -->
                    <Viewbox Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Path Data="M 4,12 L 10,18 L 20,6" 
                              Stroke="White" 
                              StrokeThickness="2.5"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"/>
                    </Viewbox>
                </Grid>

            <!-- Título -->
            <TextBlock x:Name="TitleText" Grid.Row="1" Text="Vinculado!" 
                       FontSize="26" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" Margin="0,0,0,12"/>

            <!-- Mensagem com ScrollViewer -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="280" Margin="0,0,0,24">
                <TextBlock x:Name="MessageText" 
                           Text="Dispositivo vinculado com sucesso!" 
                           FontSize="15" Foreground="#B8B8C8" TextWrapping="Wrap" TextAlignment="Center" 
                           LineHeight="24" Padding="12,0"/>
            </ScrollViewer>

            <!-- Botão com gradiente Voltris -->
            <Button Grid.Row="3" Content="Continuar" Height="48" Width="180" Click="OkButton_Click" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="14">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#FF4B6B" Offset="0"/>
                                    <GradientStop Color="#8B31FF" Offset="0.5"/>
                                    <GradientStop Color="#31A8FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                            TextElement.Foreground="White" 
                                            TextElement.FontWeight="SemiBold" 
                                            TextElement.FontSize="15"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#8B31FF" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value> 
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
        </Grid>
    </Border>
</Window>
